## Plugin Versioning
- When bumping a plugin's version, always increment **only the patch (last) component** — e.g., `1.0.1` → `1.0.2`, never `1.1.0` or `2.0.0`.
- Applies to every `version` field under `plugins/*/.claude-plugin/plugin.json`. Major/minor bumps require an explicit maintainer request.
